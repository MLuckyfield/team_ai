name: unified-platform
static_sites:
  - name: landing
    source_dir: /
    github:
      repo: MLuckyfield/team_ai
      branch: main
    build_command: mkdir -p _static && cp index.html _static/
    output_dir: _static
    routes:
      - path: / 