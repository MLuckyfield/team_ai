# Domain Configuration
DOMAIN=your-domain.com
SSL_EMAIL=your-email@domain.com

# Database Configuration (only for OpenSign metadata)
DB_USER=opensign_user
DB_PASSWORD=your_secure_password_here

# AWS S3 Configuration (for OpenSign file storage)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-opensign-bucket

# API Keys for Crawl4AI (Optional - get from respective providers)
OPENAI_API_KEY=sk-your_openai_key_here
ANTHROPIC_API_KEY=sk-ant-your_anthropic_key_here

# Application Specific
NODE_ENV=production

# Security (Generate strong passwords)
# You can generate secure passwords with: openssl rand -base64 32 